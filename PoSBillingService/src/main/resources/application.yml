server:
  port: 8888
#  servlet:
#    context-path: /api
spring:
  application:
    name: PoSBillingService
  datasource:
    url: jdbc:h2:file:./data/posbilling
    data-username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect  
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
logging:
  level:
    root: info